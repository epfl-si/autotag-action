name: 'Hello World'
description: 'Autotag'
runs:
  using: "composite"
  steps:
    - run: git log --pretty=oneline > git_log.txt
      shell: bash
    - run: cat git_log.txt
      shell: bash
    - run: node ${{ github.action_path }}/parse_log.js
      shell: bash